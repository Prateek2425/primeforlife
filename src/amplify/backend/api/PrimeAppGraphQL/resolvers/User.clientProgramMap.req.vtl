#if( $util.isNull($ctx.source.id) )
    #return
#else
    #set( $limit = $util.defaultIfNull($context.args.limit, 100) )
    #set( $page = $util.defaultIfNull($context.args.page, 1) )
    #set( $pageTmp = $page - 1 )
    #set( $offset = $pageTmp * $limit )
    {
    "version": "2018-05-29",
    "statements": [
        "SELECT * FROM ClientProgramMapping WHERE clientId = $ctx.source.id LIMIT $offset, $limit;"
    ]
}
#end